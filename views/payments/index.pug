extends ../layout

block content

  div(class="card__title clearfix")
    span Payments
    a(href="/payments/new")
      button.btn.btn--new.btn--new--bill <i class="fas fa-plus"></i> New Payment
  div(class="card__body")
    table#paymentsTable.clearfix
      thead
        tr
          th Apartment
          th Bill
          th Amount
          th Paid On
          th Action
      tbody
         each payment in payments
          tr
            td=payment.Apartment.address()
            td=payment.Bill.name
            td= payment.formattedAmount()
            td= payment.formattedCreatedAt()
            td Approve View
    div#total-payment
      div.title Total Payments: &nbsp;
      div#value

