{
  "environments": {
    "review": {
      "addons": ["heroku-postgresql:hobby-dev"],
      "scripts": {
        "postdeploy": "npm run db:create && npm run db:migrate && npm run db:seed"
      }
    }
  }
}
